<div class="card">
  
  <%= form_for @chef, html: { class: "form" } do |f| %>
    <h1 class="form-header">Edit your profile</h1>

    <%= f.label :first_name %>
    <%= f.text_field :first_name %><br>

    <%= f.label :last_name %>
    <%= f.text_field :last_name %><br>

    <%= f.label :email_address %>
    <%= f.text_field :email_address %><br>

    <%= f.label :password %>
    <%= f.password_field :password %><br>

    <%= f.label "Confirm password" %>
    <%= f.password_field :password_confirmation %><br>

    <% if flash[:errors] %>
      <ul>
        <% flash[:errors].each do |error| %>
          <li class="error"><%= error %></li>
        <% end %>
      </ul>
    <% end %>

    <%= f.submit "Save", class: "button" %>
  <% end %></p>

<hr>

<h2>🚨 Do you want to close your account? 🚨</h2>
<%= button_to "Close account", @chef, method: :delete, data: {confirm: "Are you sure?"}, class: "button-danger" %></p>
</div>